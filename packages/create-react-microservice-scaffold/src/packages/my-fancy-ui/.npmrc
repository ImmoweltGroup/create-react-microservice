#
# Due to the Immonet NPM registry being outdated and not supporting package scopes,
# we need to add package scope pointers to the default npm registry to be able to install them.
#
@unfold:registry=https://registry.npmjs.org/
