{
  "name": "{{=it.npmScope.raw}}{{=it.name.kebabCase}}",
  "license": "{{=it.license.raw}}",
  "engines": {
    "node": ">=8.9.0",
    "yarn": ">=1.3.2"
  },
  "scripts": {
    "bootstrap": "exit 0",
    "test": "exit 0"
  }
}
