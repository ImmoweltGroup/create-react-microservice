sudo: false
language: node_js
cache:
  directories:
    - node_modules
notifications:
  email: false
install:
  - yarn install
  - yarn bootstrap
script:
  - yarn run build
  - yarn run test
  - ./build/integration-test.sh
